# PasswordTool.Api

Optional ASP.NET Core Minimal API wrapper around `PasswordTool.Core`.

The API is scaffolded as a future boundary only. It should never duplicate hashing logic or return/log raw passwords.
