# PasswordTool.Core

Reusable password hashing library.

This project contains contracts, models, options, algorithm classes, inspection, registry, and security helpers. UI and API projects call this project; this project does not call them.
