# PasswordTool.WinForms

Local desktop UI for hashing, verifying, and inspecting password hashes.

This project must not contain password hashing logic directly. It should use `PasswordTool.Core` interfaces and services.
